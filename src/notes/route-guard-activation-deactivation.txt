Route Guard

-->> canActivate
    -- allow us to determine whether or not a user can navigate to a Route 
    -- look at references on (routes.ts) and (event-route-activator.service.ts) --> they use service
    for this one

-->> canDeactivate
    -- example at (route.ts) and (app.module.ts)
    